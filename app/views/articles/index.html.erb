<h1 class="text-2xl font-bold mb-4">Software Dev</h1>
<ul class="space-y-2">
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article.slug),
                  class: "text-blue-600 hover:underline" %>
    </li>
  <% end %>
</ul>
