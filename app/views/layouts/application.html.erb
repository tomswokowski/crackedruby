<!DOCTYPE html>
<html>
  <head>
    <title><%= ["CrackedRuby", content_for?(:title) ? yield(:title) : nil].compact.join(" – ") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="icon" href="/logo.png" type="image/png">
    <link rel="apple-touch-icon" href="/logo.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "marksmith", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex flex-col min-h-screen">
    <%= render "shared/nav" %>

    <div class="flex flex-1 pt-16 md:pt-0">
      <main class="flex-1 md:ml-64 md:transition-all md:duration-200 md:ease-in-out py-4 overflow-x-hidden sidebar-collapsed:md:ml-16">
        <%= yield %>
      </main>
    </div>

    <footer class="text-center text-sm text-gray py-2">
      <div class="mb-1 space-x-2">
        <%= link_to "Terms", terms_path, class: "hover:underline" %>
        <span>•</span>
        <%= link_to "Privacy", privacy_path, class: "hover:underline" %>
      </div>
      &copy; <%= Time.current.year %> CrackedRuby
    </footer>
  </body>
</html>
