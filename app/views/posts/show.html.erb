<h1 class="text-3xl font-bold mb-6"><%= @post.title %></h1>

<div class="prose lg:prose-xl mx-auto">
  <%== marksmithed(@post.body) %>
</div>

<%= link_to "← Back to Blog",
            posts_path,
            class: "mt-6 inline-block text-blue-600 hover:underline" %>
